# Maintainer-only R&D operations

Read only when editing or releasing this Skill.

Run `scripts/self_test.py`, the integration regression corpus, Cleanup strict promotion, `learning_gate.py`, `build_topic_index.py`, Cleanup's `check_context_budget.py` against `profiles/context-budget.json`, skill-creator quick validation, revision verify, and private/public sync preflight after the last modification. Public sync is private → public only and never implied by local edits.

Keep legacy references and migration source snapshots hash-addressed until routing parity, critical recall, privacy, budget, and forward canary gates are closed. Schema changes require positive, near-miss negative, malformed, stale-hash, and concurrent-writer fixtures. Never claim a model, router, or learning system is stronger merely because its prompt is shorter.
